// ============================================================
// EdgeGuard 决策树模型（由 train_model.py 自动生成）
// 推理流程：
//   1. 对原始特征做标准化：scaled[i] = (raw[i] - scaler_mean[i]) / scaler_std[i]
//   2. 从节点0开始遍历树：if scaled[feature] > threshold 走右边，否则走左边
//   3. 到叶子节点(feature==-2)时，leaf_class 就是结果 (0=normal, 1=abnormal)
// ============================================================
#define TREE_NODE_COUNT 3
#define FEATURE_COUNT   18

// 每个节点用哪个特征做判断 (-2 = 叶子节点)
const int tree_feature[3] = {
    9, -2, -2
};

// 每个节点的判断阈值（标准化后的特征值）
const float tree_threshold[3] = {
    -0.795867f, -2.000000f, -2.000000f,
};

// 每个节点的左孩子编号 (-1 = 没有)
const int tree_children_left[3] = {
    1, -1, -1,
};

// 每个节点的右孩子编号 (-1 = 没有)
const int tree_children_right[3] = {
    2, -1, -1,
};

// 叶子节点的最终分类 (0=normal, 1=abnormal)；非叶子节点填0占位
const unsigned char tree_leaf_class[3] = {
    0, 0, 1,
};

// 特征标准化参数（训练时拟合）
// 推理公式：scaled[i] = (raw_feature[i] - scaler_mean[i]) / scaler_std[i]
const float scaler_mean[18] = {
    -1196.904730f, -231.402703f, 17412.545946f, -133.605405f, 378.921622f, -14.350845f, 27423079.279155f, 24979041.993243f, 57420089.327044f, 62923997.082348f, 119334423.238716f, 8222611.030093f, 16551.905405f, 15216.689189f, 21581.023649f, 21478.398649f, 31284.949324f, 8315.368243f,
};

const float scaler_std[18] = {
    1301.416073f, 1163.327874f, 1668.271379f, 903.662987f, 1258.703445f, 556.589436f, 35135872.159052f, 31954931.934625f, 63580516.601350f, 78644309.156337f, 164852616.826748f, 13530045.152967f, 13541.768085f, 12856.031298f, 15444.983697f, 18358.663273f, 22711.211867f, 8129.148527f,
};